import { Logger } from './karma_logger';
import { ConfigOptions } from 'karma';
export declare class CaptureTimeout {
    private _launcher;
    private _config;
    private _log;
    private _pendingTimeoutId;
    constructor(_launcher: any, _config: ConfigOptions, _log: Logger);
    onStart(): void;
    onDone(): void;
}
