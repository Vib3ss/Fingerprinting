import * as webdriver from 'selenium-webdriver';
export declare class OptionsBuilder {
    static create(browserName: string | undefined, rawArgs?: string[]): webdriver.Capabilities;
    static createSafariArguments(args: string[]): string[];
    private static mapArguments;
}
