import * as webdriver from 'selenium-webdriver';
import { BrowserStackSessionCapabilities } from './browserstack_session_capabilities';
export type FirefoxProfile = ReadonlyArray<readonly [string, string | number | boolean]>;
export declare class WebDriverFactory {
    private static browserStackUrl;
    static createFromOptions(options: webdriver.Capabilities, sessionCapabilities: BrowserStackSessionCapabilities, firefoxProfile?: FirefoxProfile): Promise<webdriver.WebDriver>;
}
