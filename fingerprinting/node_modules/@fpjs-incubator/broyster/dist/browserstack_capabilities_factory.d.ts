import { BrowserStackSessionCapabilities } from './browserstack_session_capabilities';
import { BrowserStackCredentials } from './browserstack_helpers';
export declare class BrowserStackCapabilitiesFactory {
    private _credentials;
    private _local;
    constructor(_credentials: BrowserStackCredentials, _local?: boolean);
    create(browserName: string | undefined, buildName: string, sessionName: string, projectName: string, deviceName: string | undefined, os: string | undefined, idleTimeout: number, osVersion?: string | undefined, browserVersion?: string | null | undefined, localIdentifier?: string | undefined): BrowserStackSessionCapabilities;
}
